[project]
name = "mia-ragas-scorer"
version = "0.1.0"
description = "RAGAS scoring for evals/run.py, isolated from the app (RAGAS needs openai<3; the app uses openai 3.x)."
requires-python = ">=3.12,<3.13"
dependencies = [
    "ragas==0.4.3",
    "azure-identity>=1.25",
    "langchain-community==0.4.1",
]

[tool.uv]
package = false
